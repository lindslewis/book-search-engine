// hint in readme says to use the functionality in user-controller.js as a guide....

// these are both called at the top of user-controller
const { User } = require('../models');
const { signToken } = require('../utils/auth');


const resolvers = {

}